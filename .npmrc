# Detiene la instalacion si la version de Node no es compatible, en lugar de
# dejar que falle mas tarde: better-sqlite3 en una version de Node que no
# soporta no da un error claro, revienta con SIGSEGV al primer uso.
engine-strict=true
